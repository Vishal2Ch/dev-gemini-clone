apiVersion: v1
kind: Service
metadata:
  name: gemini-service
  namespace: gemini-namespace
  labels:
    app: gemini
spec:
  selector:
    app: gemini
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
  type: ClusterIP
    # nodePort is auto-assigned (e.g., 30194)
